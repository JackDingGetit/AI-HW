任务一：
    论文来源：arxiv官网
    导读生成方式；trae ide
    配图方式；手动截图

任务二：
采用的API：Doubao-Seed-1.8
运行方式：终端
注意事项：
鉴权与接入点（BaseURL）

鉴权：调用前，你需要在火山引擎控制台生成API Key，并建议将其配置为环境变量以确保安全 。

接入点：API的BaseURL会根据你使用的平台和地域有所不同。例如，火山引擎的BaseURL通常是 https://operator.las.volces.com/... 这样的格式，并且地域（如 cn-beijing）是路径的一部分 。如果你通过第三方平台（如CometAPI）调用，则需要使用该平台提供的专用BaseURL 。

多模态输入（图像与视频）
这是该模型的核心能力之一。你可以通过messages接口同时传入文本和视觉数据。

数据源：支持多种方式提供图片或视频，包括公网URL、Base64编码或本地文件 。

视频理解：模型能力很强，官方宣称可以一次分析最多1280帧视频 。在API调用时，通过video_url或video_base64等字段传入即可 。

深度思考模式（Reasoning）
模型具备“思维链”能力，在给出最终答案前会进行内部推理。

输出字段：模型的最终回答在llm_result字段中，而其推理过程则单独输出在reasoning_content字段里 。这对于调试模型逻辑或向用户展示思考过程很有帮助。

控制开关：你可以通过thinking或thinking_type等参数来控制是否开启深度思考模式 。

参数设置注意事项

最大输出长度：注意区分两个参数。max_tokens控制模型回答的长度（不含思维链） 。而max_completion_tokens控制回答+思维链的总长度，二者不可同时设置 。

不支持的参数：根据官方文档，深度思考能力模型不支持 stop（停止词）、logprobs（对数概率）和 logit_bias（概率偏置）这几个参数 。