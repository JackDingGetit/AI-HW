# Python 版本要求 / Python Version Requirements
# Python 3.8 - 3.13

# 核心依赖 / Core Dependencies
tensorflow>=2.10.0
numpy>=1.23.0
matplotlib>=3.5.0
scikit-learn>=1.1.0
seaborn>=0.11.0
Pillow>=9.0.0

# 可选: GPU 加速 (如使用 NVIDIA GPU)
# Optional: GPU acceleration (for NVIDIA GPUs)
# tensorflow-gpu>=2.10.0
# cudnn>=8.1.0
